let cores = ["Amarelo", "Azul", "Vermelho", "Cinza", "Preto", "Branco"];
let coresFiltrada = cores.filter(cor => cor.length > 4);

console.log(`Cores filtrada(cores com mais de 4 letras): ${coresFiltrada}`);
